server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/service_monitor
    username: postgres #<-- Ваш никнейм
    password: postgres #<-- Пароль
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
  flyway:
    enabled: true

app:
  monitoring:
    # Every minute at second 0
    cron: "0 * * * * *"
    request-timeout: 5s
    connect-timeout: 3s
    user-agent: "service-monitor/0.0.1"
    max-body-bytes: 4096
